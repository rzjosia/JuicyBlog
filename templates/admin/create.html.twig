{% extends 'base.html.twig' %}

{% form_theme formView 'materialize_layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s6">
                {{ form(formView) }}
            </div>
        </div>
        <p class="align-right"><a href="{{ path('home') }}">Retour</a></p>
    </div>
{% endblock body %}